#!/bin/bash

source /usr/bin/ui.sh

# //====================================================
# //  Newbie Store - Copy 2024
# //====================================================
# //  System Request : DEB 9-12 / UBUNTU 18-24
# //  Author         : Kurniawan Setiadi
# //  Develop        : Newbie Store
# //  email          : Newbiestore09@gmail.com
# //  telegram       : https://t.me/newbie_store24
# //====================================================
MYIP=$(wget -qO- icanhazip.com);
MAX_RETRY=3
RETRY_FILE="/tmp/retry_getotp"
if [[ ! -f "$RETRY_FILE" ]]; then
    echo 0 > "$RETRY_FILE"
fi
RETRY_COUNT=$(cat "$RETRY_FILE")
garis_bawah(){
print_rainbow " └──────────────────────────────────────────────┘"
}
garis_atas(){
print_rainbow " ┌──────────────────────────────────────────────┐"
}
function Newbie_Banner_Log() {
BOX_WIDTH=48
TEXT="[ GET AUTH TOKEN BERHASIL ] "
TEXT_LENGTH=${#TEXT}
LEFT_PADDING=$(( (BOX_WIDTH - TEXT_LENGTH) / 2 ))
RIGHT_PADDING=$(( BOX_WIDTH - TEXT_LENGTH - LEFT_PADDING ))
LEFT_SPACE=$(printf '%*s' $LEFT_PADDING '')
RIGHT_SPACE=$(printf '%*s' $RIGHT_PADDING '')
print_rainbow " ════════════════════════════════════════════════"
print_rainbow " ${LEFT_SPACE}${TEXT}${RIGHT_SPACE}"
print_rainbow " ════════════════════════════════════════════════"
}
ipsaya=$(curl -sS ipv4.icanhazip.com)
data_server=$(curl -v --insecure --silent https://google.com/ 2>&1 | grep Date | sed -e 's/< Date: //')
date_list=$(date +"%Y-%m-%d" -d "$data_server")
data_ip="https://raw.githubusercontent.com/kertasbaru/izin/main/ip"
checking_sc() {
  useexp=$(wget -qO- $data_ip | grep -wE $ipsaya | awk '{print $3}')

  if [[ "$useexp" == "Lifetime" ]]; then
    # Jika useexp adalah "Lifetime", anggap tetap aktif

    return
  fi
  date_list_epoch=$(date -d "$date_list" +%s)
  useexp_epoch=$(date -d "$useexp" +%s 2>/dev/null)

  if [[ $? -ne 0 || $date_list_epoch -gt $useexp_epoch ]]; then
    # Tanggal sekarang lebih besar dari tanggal expired, atau format salah
echo -e "${YELLOW}────────────────────────────────────────────${RESET}"
echo -e "${BRED}${BOLD} ⚠️       AKSES DI TOLAK         ⚠️ ${RESET}"
echo -e "${YELLOW}────────────────────────────────────────────${RESET}"
echo -e ""
echo -e "        ${RED}❌ SCRIPT LOCKED ❌${RESET}"
echo -e ""
echo -e "  ${YELLOW}🔒 Your VPS${RESET} $ipsaya ${YELLOW}Has been Banned${RESET}"
echo -e ""
echo -e "  ${RED}⚠️  Masa Aktif Sudah Habis ⚠️${RESET}"
echo -e "  ${YELLOW}💡 Beli izin resmi hanya dari Admin!${RESET}"
echo -e ""
echo -e "  ${GREEN}📞 Contact Admin:${RESET}"
echo -e "  ${CYAN}🌍 Telegram: https://t.me/WuzzSTORE${RESET}"
echo -e "  ${CYAN}📱 WhatsApp: https://wa.me/6287760204418${RESET}"
echo -e ""
echo -e "${YELLOW}────────────────────────────────────────────${RESET}"
exit 1
  fi
}
checking_sc
eval $(wget -qO- https://domainsaya.dekaa.my.id)
if ! command -v jq &> /dev/null; then
    echo -e "Tunggu sebentar proses. Prepaired..."
    apt install jq -y >/dev/null
else
    echo -e "Persiapan Selesai."
fi
domain=$(cat /etc/xray/domain)
username=$(wget -qO- $data_ip | grep $ipsaya | awk '{print $2}')
clear
figlet "NEWBIE STORE" | lolcat

API_ID_FILE="/etc/api_telegram_id"

if [[ -f "$API_ID_FILE" && -s "$API_ID_FILE" ]]; then
  TELEGRAM_ID=$(cat "$API_ID_FILE")
  echo -e "\e[1;32m✅ ID Telegram ditemukan:${RESET} \e[1;36m$TELEGRAM_ID${RESET}"
  echo -e "\e[1;33m⏩ Melewati input ID dan lanjut eksekusi...${RESET}"
else
  echo -e ""
  echo -e "\e[1;36m✦ Selamat datang di Kunci Server Newbie Store!${RESET}"
  print_rainbow " ════════════════════════════════════════════════"
  echo -e "🛡️  Sistem ini akan membantu Mengirim Auth Key VPS Anda."
  echo -e "📌 Silakan masukkan \e[1;32mID Telegram Anda${RESET} untuk menerima notifikasi Auth Key."
  echo -e "📩 Jangan lupa chat bot berikut dulu agar bisa menerima pesan:"
  echo -e "👉 \e[1;34mhttps://t.me/ServernewbieBot${RESET}"
  print_rainbow " ════════════════════════════════════════════════"

  read -p $'\e[1;36m🔹 Masukkan ID Telegram Anda: ${RESET}' TELEGRAM_ID
  echo "$TELEGRAM_ID" > "$API_ID_FILE"
  echo -e "\e[1;32m✅ ID Anda berhasil disimpan! Notifikasi akan dikirim ke Telegram Anda.${RESET}"
  echo -e ""
fi
clear
CHATIDAPI=$(cat /etc/api_telegram_id)
TIMES="10"
URL="https://api.telegram.org/bot8299113844:AAFLpQunG36d7GqNw_VTF4-zn3YB_Fcov98/sendMessage"
TEXT="_______________________________
    AUTH KEY SERVER $nama  
_______________________________
  USERNAME  : $username
  DOMAIN    : <code>$domain</code> 
  AUTH KEY  : <code>$AUTH_KEY</code> 
_______________________________
"
curl -s --max-time $TIMES -d "chat_id=$CHATIDAPI&disable_web_page_preview=1&text=$TEXT&parse_mode=html" $URL >/dev/null

Newbie_Banner_Log
garis_atas
echo -e "   ${WHITE}${BOLD}  USERNAME  : $username $RESET"
echo -e "   ${WHITE}${BOLD}  DOMAIN    : $domain $RESET"
echo -e "   ${WHITE}${BOLD}  AUTH KEY  : $AUTH_KEY $RESET"
garis_bawah
echo -e "[ ${BLUE}INFO${RESET} ] Tekan sembarang tombol untuk kembali ke menu..."
read -n 1 -s -r -p ""
clear
m-bot
