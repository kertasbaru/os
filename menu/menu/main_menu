#!/bin/bash

source /usr/bin/ui.sh

# //====================================================
# //  Newbie Store - Copy 2024
# //====================================================
# //  System Request : DEB 9-12 / UBUNTU 18-24
# //  Author         : Kurniawan Setiadi
# //  Develop        : Newbie Store
# //  email          : Newbiestore09@gmail.com
# //  telegram       : https://t.me/newbie_store24
# //====================================================
clear
garis_bawah(){
print_rainbow " └──────────────────────────────────────────────┘"
}
garis_atas(){
print_rainbow " ┌──────────────────────────────────────────────┐"
}
function Loading() {
BOX_WIDTH=48
TEXT="[ PROCESSING, PLEASE WAIT.. ] "
TEXT_LENGTH=${#TEXT}
LEFT_PADDING=$(( (BOX_WIDTH - TEXT_LENGTH) / 2 ))
RIGHT_PADDING=$(( BOX_WIDTH - TEXT_LENGTH - LEFT_PADDING ))
LEFT_SPACE=$(printf '%*s' $LEFT_PADDING '')
RIGHT_SPACE=$(printf '%*s' $RIGHT_PADDING '')
print_rainbow " ════════════════════════════════════════════════"
print_rainbow " ${LEFT_SPACE}${TEXT}${RIGHT_SPACE}"
print_rainbow " ════════════════════════════════════════════════"
}

MYIP=$(curl -sS ipv4.icanhazip.com)
PROFIL=$(curl -sS https://raw.githubusercontent.com/kertasbaru/izin/main/ip | grep -wE $MYIP | awk '{print $2}')
TOKEN=$(cat /etc/github/api)
REPO="https://github.com/kertasbaru/izin/"
EMAIL=$(cat /etc/github/email)
USER=$(cat /etc/github/username)
data_ip=$(curl -sS https://raw.githubusercontent.com/kertasbaru/izin/main/ip | grep -wE $MYIP | awk '{print $6}')
checking_sc() {
if [[ -n "$data_ip" || "$data_ip" -gt 0 ]]; then
    echo -ne
  else
    # Tanggal sekarang lebih besar dari tanggal expired, atau format salah
echo -e "${YELLOW}────────────────────────────────────────────${RESET}"
echo -e "${BRED}${BOLD} ⚠️       AKSES DI TOLAK         ⚠️ ${RESET}"
echo -e "${YELLOW}────────────────────────────────────────────${RESET}"
echo -e ""
echo -e "        ${RED}❌ SCRIPT LOCKED ❌${RESET}"
echo -e ""
echo -e "  ${YELLOW}🔒 Your VPS${RESET} $ipsaya ${YELLOW}Has been Banned${RESET}"
echo -e ""
echo -e "  ${RED}⚠️  Masa Aktif Sudah Habis ⚠️${RESET}"
echo -e "  ${YELLOW}💡 Beli izin resmi hanya dari Admin!${RESET}"
echo -e ""
echo -e "  ${GREEN}📞 Contact Admin:${RESET}"
echo -e "  ${CYAN}🌍 Telegram: https://t.me/WuzzSTORE${RESET}"
echo -e "  ${CYAN}📱 WhatsApp: https://wa.me/6287760204418${RESET}"
echo -e ""
echo -e "${YELLOW}────────────────────────────────────────────${RESET}"
exit 1
  fi
}
checking_sc
# Definisi warna
y='${YELLOW}'  # Kuning
c='\033[0;36m'  # Cyan
g='${GREEN}'  # Hijau
r='${RED}'  # Merah

# Fungsi untuk menampilkan menu utama
show_menu() {
    clear
banner "MENU ADMIN"
    echo -e "${WHITE}${BOLD}  1.) Cek VPS Online${RESET}"
    echo -e "${WHITE}${BOLD}  2.) Replace IP Offline${RESET}"
    echo -e "${WHITE}${BOLD}  3.) Input Data VPS${RESET}"  # Tambahan opsi baru
    echo -e "${WHITE}${BOLD}  4.) Hubungkan VPS${RESET}"
    echo -e "${WHITE}${BOLD}  5.) Pasang SC ke VPS${RESET}"
    echo -e "${WHITE}${BOLD}  6.) Rebuild VPS${RESET}"
    echo -e "${WHITE}${BOLD}  7.) Ganti URL Izin${RESET}"
    echo -e "${WHITE}${BOLD}  8.) Hapus Data Inventory${RESET}"
    echo -e "${r}  x.) Back To Menu${RESET}"
garis
    echo -ne "${y}Pilih opsi [1-8/x]: ${RESET}"
}

Fungsi untuk input data VPS
input_data_vps() {
    clear
    echo -e "${c}Menambahkan data VPS ke file inventory...${RESET}"
    INVENTORY_FILE="/root/inventory.ini"

    if [[ ! -f $INVENTORY_FILE ]]; then
        echo "[vps_group]" > $INVENTORY_FILE
        echo -e "${y}File inventory baru dibuat.${RESET}"
    fi

    while true; do
        echo -ne "${y}Masukkan nama VPS (atau 'exit' untuk kembali): ${RESET}"
        read -r vps_name
        [[ $vps_name == "exit" ]] && break

        echo -ne "${y}Masukkan IP VPS: ${RESET}"
        read -r ansible_host

        echo -ne "${y}Masukkan domain (${r}wajib sudah terpointing!${RESET}): ${RESET}"
        read -r domain

        # Validasi input
        if [[ -z $vps_name || -z $ansible_host || -z $domain ]]; then
            echo -e "${r}Data tidak boleh kosong. Coba lagi.${RESET}"
            continue
        fi

        echo "${vps_name} ansible_host=${ansible_host} ansible_user=root domain=${domain} ansible_python_interpreter=/usr/bin/python3" >> "$INVENTORY_FILE"
        echo -e "${WHITE}${BOLD}Data VPS berhasil ditambahkan.${RESET}\n"
        sleep 0.5 && clear
    done

    echo -e "${WHITE}${BOLD}Selesai menambahkan data VPS.${RESET}"
    sleep 0.5
    menu_ftr
}

hapus_inventory() {
    INVENTORY_FILE="/root/inventory.ini"
    if [[ -f $INVENTORY_FILE ]]; then
        rm "$INVENTORY_FILE"
        echo -e "${WHITE}${BOLD}File inventory telah dihapus.${RESET}"
    else
        echo -e "${r}File inventory tidak ditemukan.${RESET}"
    fi
    echo -e "\n${WHITE}${BOLD}Tekan Enter untuk kembali ke menu...${RESET}"
    read -r
    menu_ftr
}
# Fungsi untuk cek VPS online
cek_vps() {
    cek_vps_online
    echo -e "\n${WHITE}${BOLD}Tugas selesai. Tekan Enter untuk kembali ke menu...${RESET}"
    read -r
    menu_ftr
}

# Fungsi untuk replace IP offline
replace_ip_offline() {
    repip
}

# Fungsi untuk ganti URL izin
ganti_url_izin() {
    if [ -f "/root/.url" ]; then
        rm /root/.url
        echo -e "${WHITE}${BOLD}URL izin telah dihapus. Tekan Enter untuk kembali ke menu...${RESET}"
    else
        echo -e "${r}File /root/.url tidak ditemukan. Tidak ada URL yang dihapus.${RESET}"
        echo -e "${r}Tekan Enter untuk kembali ke menu...${RESET}"
    fi
    read -r
    menu_ftr
}

# Fungsi untuk hubungkan VPS
hubungkan_vps() {
    clear
    # File inventory
    INVENTORY_FILE="/root/inventory.ini"

    # Memastikan sshpass sudah terinstal
    if ! command -v sshpass &> /dev/null; then
        echo -e "${y}sshpass tidak ditemukan. Menginstal sshpass...${RESET}"
        apt update -y &> /dev/null
        apt install sshpass -y &> /dev/null
        echo -e "${WHITE}${BOLD}sshpass berhasil diinstal.${RESET}"
    fi

    # Menghapus entri known_hosts untuk memastikan koneksi sukses
    echo -e "${c}Menghapus semua entri SSH known_hosts...${RESET}"
    rm -f /root/.ssh/known_hosts

    # Memeriksa apakah file inventory ada
    if [[ ! -f $INVENTORY_FILE ]]; then
        echo -e "${r}File inventory tidak ditemukan: $INVENTORY_FILE${RESET}"
        return
    fi

# Menanyakan apakah semua VPS memiliki password yang sama
echo -ne "${y}Apakah semua VPS memiliki password yang sama? (Y/n): ${RESET}"
read -r same_password

if [[ "${same_password,,}" == "y" ]]; then
    # Jika semua VPS memiliki password yang sama
    while true; do
        echo -ne "${y}Masukkan password untuk semua VPS: ${RESET}"
        read -rs password
        echo ""

        # Memastikan password tidak kosong
        if [[ -z "$password" ]]; then
            echo -e "${r}Password tidak boleh kosong. Silakan coba lagi.${RESET}"
            continue
        fi

        # Menghubungkan ke semua VPS
        echo -e "${c}Menghubungkan ke semua VPS yang terdaftar...${RESET}"

        while IFS= read -r line; do
            # Skip baris kosong, komentar, atau grup
            [[ -z $line || $line =~ ^# || $line =~ ^\[.*\]$ ]] && continue

            # Memecah data inventory berdasarkan pola
            vps_name=$(echo "$line" | awk '{print $1}')
            ansible_host=$(echo "$line" | grep -oP 'ansible_host=\K\S+')
            ansible_user=$(echo "$line" | grep -oP 'ansible_user=\K\S+')

            echo -e "${y}Menghubungkan ke VPS: ${vps_name} (${ansible_host})...${RESET}"

            sshpass -p "$password" ssh-copy-id -o StrictHostKeyChecking=no "${ansible_user}@${ansible_host}"
            if [[ $? -ne 0 ]]; then
                echo -e "${r}Gagal menghubungkan ke ${vps_name}. Silakan periksa password atau konfigurasi VPS.${RESET}"
                continue
            fi
        done < "$INVENTORY_FILE"

        echo -e "${WHITE}${BOLD}Proses selesai! Semua VPS telah terhubung.${RESET}"
        break
    done
else
    # Jika password berbeda, meminta user memilih VPS dan memasukkan password manual
    echo -e "${y}Pilih VPS yang ingin Anda hubungkan: ${RESET}"

    # Menampilkan daftar VPS
    declare -A vps_data
    index=0
    while IFS= read -r line; do
        # Skip baris kosong, komentar, atau grup
        [[ -z $line || $line =~ ^# || $line =~ ^\[.*\]$ ]] && continue

        # Memecah data inventory berdasarkan pola
        vps_name=$(echo "$line" | awk '{print $1}')
        ansible_host=$(echo "$line" | grep -oP 'ansible_host=\K\S+')
        ansible_user=$(echo "$line" | grep -oP 'ansible_user=\K\S+')

        # Simpan data VPS dalam associative array
        vps_data["$index,name"]="$vps_name"
        vps_data["$index,host"]="$ansible_host"
        vps_data["$index,user"]="$ansible_user"

        ((index++))
        echo "$index) $vps_name"
    done < "$INVENTORY_FILE"

    # Meminta pengguna memilih VPS
    echo -ne "${y}Masukkan nomor VPS yang ingin Anda hubungkan: ${RESET}"
    read -r vps_choice

    # Validasi input pengguna
    if [[ ! "$vps_choice" =~ ^[0-9]+$ ]] || [ "$vps_choice" -le 0 ] || [ "$vps_choice" -gt "$index" ]; then
        echo -e "${r}Pilihan tidak valid.${RESET}"
        exit 1
    fi

    selected_vps_index=$((vps_choice - 1))
    selected_vps="${vps_data["$selected_vps_index,name"]}"
    selected_host="${vps_data["$selected_vps_index,host"]}"
    selected_user="${vps_data["$selected_vps_index,user"]}"

    echo -e "${y}Anda memilih VPS: ${selected_vps}${RESET}"

    while true; do
        echo -ne "${y}Masukkan password untuk VPS ${selected_vps}: ${RESET}"
        read -rs password
        echo ""

        if [[ -z "$password" ]]; then
            echo -e "${r}Password tidak boleh kosong. Silakan coba lagi.${RESET}"
            continue
        fi

        sshpass -p "$password" ssh-copy-id -o StrictHostKeyChecking=no "${selected_user}@${selected_host}"
        if [[ $? -eq 0 ]]; then
            echo -e "${WHITE}${BOLD}Berhasil menghubungkan ke VPS ${selected_vps}.${RESET}"
            break
        else
            echo -e "${r}Password salah atau koneksi gagal. Silakan coba lagi.${RESET}"
        fi
    done
fi

echo -e "\nTekan Enter untuk kembali ke menu..."
    read -r
menu_ftr

}

# Fungsi untuk pasang SC ke VPS
pasang_sc_ke_vps() {
clear
    # Cek apakah file inventory.ini ada
    if [[ ! -f /root/inventory.ini ]]; then
        echo -e "${r}File inventory.ini tidak ditemukan.${RESET}"
        echo -e "${y}Silakan atur penggantian VPS dan hubungkan terlebih dahulu sebelum melanjutkan.${RESET}"
        return
    fi

    # Cek apakah file install.yml ada, jika tidak buat file baru
    if [[ ! -f /root/install.yml ]]; then
        echo -e "${y}File install.yml tidak ditemukan, membuat file baru...${RESET}"
        cat <<EOF > /root/install.yml
---
- name: Install script on multiple VPS without prompts
  hosts: vps_group  # Menggunakan grup vps_group
  become: true  # Menjalankan playbook sebagai root
  environment:
    DEBIAN_FRONTEND: noninteractive  # Menghindari prompt selama instalasi
  vars:
    default_domain: example.t.me/WuzzSTORE  # Default domain jika tidak didefinisikan di inventory
  tasks:
    # Instalasi dependensi tanpa prompt
    - name: Install dependencies (wget, screen, curl, openssh-server, jq)
      apt:
        name:
          - wget
          - screen
          - curl
          - openssh-server
          - jq
        state: present
        update_cache: yes
    # Install python3-apt untuk kompatibilitas Ansible
    - name: Install python3-apt to ensure Ansible works
      apt:
        name: python3-apt
        state: present
        update_cache: yes
    # Unduh skrip Install.sh
    - name: Download Install script
      get_url:
        url: https://raw.githubusercontent.com/kertasbaru/vip/main/Install.sh
        dest: /root/Install.sh
    # Berikan izin eksekusi pada skrip
    - name: Make Install script executable
      file:
        path: /root/Install.sh
        mode: '0755'
    # Jalankan skrip dalam sesi screen dengan input otomatis
    - name: Run Install.sh in screen with predefined input
      shell: |
        screen -S install -dm bash -c "/root/Install.sh {{ inventory_hostname }} {{ domain | default(default_domain) }}"
EOF
    fi

    # Pastikan semua VPS memiliki Ansible Playbook
    echo -e "${c}Memeriksa apakah Ansible Playbook sudah terinstal di VPS...${RESET}"
    ansible-playbook --version &> /dev/null
    if [[ $? -ne 0 ]]; then
        echo -e "${y}Ansible Playbook tidak ditemukan. Memasang Ansible Playbook...${RESET}"
        apt update -y &> /dev/null
        apt install ansible -y &> /dev/null
    fi

    # Pastikan python3-apt terinstal
    echo -e "${c}Memeriksa apakah python3-apt terinstal di VPS...${RESET}"
    apt list --installed | grep -q python3-apt || apt install python3-apt -y &> /dev/null

    # Jalankan playbook untuk memasang SC
    echo -e "${c}Menjalankan playbook untuk memasang script pada VPS...${RESET}"
    ansible-playbook -i /root/inventory.ini /root/install.yml

    echo -e "${WHITE}${BOLD}Pemasangan script selesai.${RESET}"
    echo -e "\n${WHITE}${BOLD}Tugas selesai. Tekan Enter untuk kembali ke menu...${RESET}"
    read -r
    menu_ftr
}
rebuild_vps() {
    clear
    # Cek apakah file inventory.ini ada
    if [[ ! -f /root/inventory.ini ]]; then
        echo -e "${r}File inventory.ini tidak ditemukan.${RESET}"
        echo -e "${y}Silakan tambahkan inventory terlebih dahulu sebelum melanjutkan.${RESET}"
        return
    fi

    # Tampilkan daftar VPS dari inventory.ini
    echo -e "${c}Daftar VPS yang tersedia:${RESET}"
    mapfile -t vps_list < <(grep -E 'ansible_host=' /root/inventory.ini | awk '{print $1, $2}')
    if [[ ${#vps_list[@]} -eq 0 ]]; then
        echo -e "${r}Tidak ada VPS yang ditemukan di inventory.ini.${RESET}"
        return
    fi

    for i in "${!vps_list[@]}"; do
        vps_name=$(echo "${vps_list[i]}" | awk '{print $1}')
        vps_ip=$(echo "${vps_list[i]}" | awk -F'=' '{print $2}')
        echo "$((i+1)). $vps_name ($vps_ip)"
    done

    # Pilih VPS
    echo -ne "${y}Pilih VPS yang ingin di-rebuild [1-${#vps_list[@]}]: ${RESET}"
    read -r vps_choice
    if [[ ! $vps_choice =~ ^[0-9]+$ ]] || (( vps_choice < 1 || vps_choice > ${#vps_list[@]} )); then
        echo -e "${r}Pilihan tidak valid.${RESET}"
        return
    fi

    selected_vps=$(echo "${vps_list[vps_choice-1]}" | awk '{print $1}')
    selected_ip=$(grep -E "^${selected_vps}" /root/inventory.ini | awk '{print $2}' | cut -d'=' -f2)
    clear
    echo -e "${c}VPS terpilih: $selected_vps ($selected_ip)${RESET}"

    # Pilih OS yang ingin digunakan
    echo -e "${c}Pilih OS untuk VPS:${RESET}"
    echo "1. Ubuntu 20.04"
    echo "2. Ubuntu 22.04"
    echo "3. Ubuntu 24.04"
    echo "4. Debian 10"
    echo "5. Debian 11"
    echo "6. Debian 12"
    echo -ne "${y}Masukkan pilihan OS [1-6]: ${RESET}"
    read -r os_choice

    case $os_choice in
        1) os_selected="Ubuntu 20.04" ;;
        2) os_selected="Ubuntu 22.04" ;;
        3) os_selected="Ubuntu 24.04" ;;
        4) os_selected="Debian 10" ;;
        5) os_selected="Debian 11" ;;
        6) os_selected="Debian 12" ;;
        *) 
            echo -e "${r}Pilihan tidak valid.${RESET}"
            return
            ;;
    esac

    # Meminta input password baru untuk VPS
    echo -ne "${y}Masukkan password baru untuk root VPS: ${RESET}"
    read -s new_password
    echo

    # Validasi password (minimal 4 karakter)
    if [[ ${#new_password} -lt 4 ]]; then
        echo -e "${r}Password terlalu pendek. Minimal 4 karakter.${RESET}"
        return
    fi
clear
    # Rebuild VPS dengan OS terpilih dan kata sandi baru
    echo -e "${c}Memulai reinstall OS pada VPS $selected_vps ($selected_ip)...${RESET}"
    ssh root@"$selected_ip" "curl -O https://raw.githubusercontent.com/bin456789/reinstall/main/reinstall.sh && \
        printf '${new_password}\n${new_password}\n' | bash reinstall.sh ${os_selected} && reboot"

    echo -e "${WHITE}${BOLD}Proses reinstall selesai. VPS akan reboot.${RESET}"
    echo -e "\n${WHITE}${BOLD}Tugas selesai. Tekan Enter untuk kembali ke menu...${RESET}"
    read -r
    menu_ftr
}
# Fungsi lainnya tetap sama, hanya perubahan nomor menu
menu_ftr() {
    show_menu
    read -r option
    case $option in
        1)
            cek_vps
            ;;
        2)
            replace_ip_offline
            ;;
        3)
            input_data_vps  # Tambahan opsi input data VPS
            ;;
        4)
            hubungkan_vps
            ;;
        5)
            pasang_sc_ke_vps
            ;;
        6)
            rebuild_vps
            ;;
        7)
            ganti_url_izin
            ;;
        8)
            hapus_inventory
            ;;
        x)
            echo -e "${y}Kembali ke menu utama...${RESET}"
            menu
            ;;
        *)
            echo -e "${r}Pilihan tidak valid, coba lagi.${RESET}"
            menu_ftr
            ;;
    esac
}

# Menampilkan menu utama
menu_ftr
